{
  "name": "primordia-site-dev",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check scripts/build-static.mjs && node --check scripts/cohort-showcase.js && node --check scripts/hero.js && node --check scripts/site-pages.js && node --check scripts/application.js && node --check prototype/form.mjs && node --check backend/intake.mjs && node --check backend/airtable.mjs && node --check netlify/functions/application.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
