/* ============================================================
   PRIMORDIA — SPACING, RADIUS, BORDERS, MOTION
   Hard rules, radius zero. Paper, not glass.
   Motion is a cut: instant, or 100ms linear. No spring, no bounce.
   ============================================================ */

:root {
  /* ---------- SPACING SCALE (4px base) ---------- */
  --s-0:  0;
  --s-1:  4px;
  --s-2:  8px;
  --s-3:  12px;
  --s-4:  16px;
  --s-5:  24px;
  --s-6:  32px;
  --s-7:  48px;
  --s-8:  64px;
  --s-9:  96px;
  --s-10: 128px;

  /* ---------- SECTION RHYTHM ---------- */
  --pad-section: clamp(56px, 8vw, 90px); /* vertical padding per page section */
  --pad-gutter:  clamp(20px, 4vw, 40px); /* page edge gutter */
  --maxw:        1180px;                 /* content max width */
  --nav-h:       56px;                   /* fixed nav bar height */

  /* ---------- CORNER RADIUS ----------
     Zero everywhere. Single exception: a full circle for the mark. */
  --radius:       0;
  --radius-round: 9999px;

  /* ---------- BORDER WIDTHS ---------- */
  --bw-hairline: 1px;
  --bw:          2px;
  --bw-thick:    4px;

  /* ---------- MOTION ----------
     Default is instant. Snap is the only easing — 100ms linear. */
  --motion-instant: 0ms;
  --motion-snap:    100ms;
  --ease:           linear;
}
